db.createUser( { user: "myNormalUser", pwd: "xyz123", roles: [{ role: "readWrite", db: "some_db" }, { role: "read", db: "some_other_db" }] })
use some_db
ls
use some_db
db.createCollection("products") 
help
db.createCollection("products")
use admin
find()
db.find()
db.getUsers()
db.createCollection("products")
docker create products
lsdb.createCollection("products")
docker create products
lsts")
docker create products
ls